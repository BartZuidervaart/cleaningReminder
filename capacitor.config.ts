import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'cleaning-reminder',
  webDir: 'dist/cleaningReminder',
  bundledWebRuntime: false
};

export default config;
